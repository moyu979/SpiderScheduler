# 是否执行更新数据库操作
do_update: false

# 更新线程数
update_thread: 1

# 每轮更新的限制，当满足任意一条时，会在完成这次更新后停止更新
# 每次更新的用户数量（单位：个），-1表示无限制
once_update_number: -1
# 更新持续时间（单位：秒），-1表示无限制
once_update_duration: -1

# 每轮更新的间隔（单位：秒），-1表示无限制，此时等效于持续更新
update_interval: 86400
